# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'rubygems-changelog' do
  developer('Lars Henrik Mai', 'lars.mai@kontinui.de')

  dependency('mocha', '>= 0.9.12', :dev)
  dependency('autotest-standalone', '>= 4.5.5', :dev)

  require_rubygems_version('>= 1.8.5')
end

# vim: syntax=ruby
